# Model adapter weights (too large for git)
sft_xyz_adapter/
grpo_xyz_adapter/

# ChromaDB vector store (binary, regenerate with ingest)
chroma_db/

# Personal documents
ac.pdf

# Claude internal
.claude/

# Logs (can be large, regenerated by running notebook)
inference_log.jsonl
query_log.jsonl
